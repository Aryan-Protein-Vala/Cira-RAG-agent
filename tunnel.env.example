# Copy to tunnel.env (git-ignored) and fill in. Used by start_ssh_tunnel.bat.
# Never put a password in a file: it is requested interactively by ssh.
SSH_USER=
SSH_HOST=
# The database host *as seen from the jump machine* — a private name or IP.
HANA_HOST=
HANA_PORT=30013
SERVICE_LAYER_PORT=50000
