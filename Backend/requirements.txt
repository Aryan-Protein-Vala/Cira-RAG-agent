# CIRA backend runtime dependencies.
#
# NOT on PyPI: the SAP HANA driver. Install the SAP HANA Client (which provides
# hdbcli) from https://help.sap.com/viewer/p/SAP_HANA_CLIENT first — e.g.
#   python -m pip install "hdbcli==2.21.*" --index-url <your SAP artifact index>
# or, after installing the client package:
#   python -m pip install ./hdbcli-2.21.*.whl
# Without hdbcli CIRA still boots; it just reports that HANA is unavailable.

# ── Web framework ────────────────────────────────────────────────────────────
fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.7
python-multipart>=0.0.9          # required by FastAPI for file uploads

# ── Persistence ──────────────────────────────────────────────────────────────
SQLAlchemy>=2.0                  # async engine + ORM for chat history
aiosqlite>=0.20                  # async SQLite driver used by SQLAlchemy
greenlet>=3.0.0                  # required by SQLAlchemy async

# ── Agent / LLM ──────────────────────────────────────────────────────────────
langchain>=0.3
langchain-core>=0.3
langchain-openai>=0.2
langgraph>=0.2

# ── SAP connectivity ─────────────────────────────────────────────────────────
hdbcli>=2.19                     # SAP HANA client — see the note at the top
httpx>=0.27                      # SAP B1 Service Layer (OData, port 50000) + /transcribe
pymssql>=2.2.0                   # Microsoft SQL Server (port 1433); only needed for MSSQL_*

# ── Misc ─────────────────────────────────────────────────────────────────────
python-dotenv>=1.0
cryptography>=42.0               # encrypts admin-panel SAP credentials at rest (credential_store.py)
