# ── Web framework ────────────────────────────────────────────────────────────
fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.7
python-multipart>=0.0.9          # required by FastAPI for file uploads

# ── Persistence ──────────────────────────────────────────────────────────────
SQLAlchemy>=2.0                  # was missing: the app could not start without it
aiosqlite>=0.20                  # async SQLite driver used by SQLAlchemy

# ── Agent / LLM ──────────────────────────────────────────────────────────────
langchain>=0.3
langchain-core>=0.3
langchain-openai>=0.2
langgraph>=0.2

# ── SAP connectivity ─────────────────────────────────────────────────────────
hdbcli>=2.19                     # SAP HANA client (direct SQL, port 3<inst>15/30013)
httpx>=0.27                      # SAP B1 Service Layer (OData, port 50000)
pymssql>=2.2.0                   # Microsoft SQL Server client (port 1433)

# ── Misc ─────────────────────────────────────────────────────────────────────
python-dotenv>=1.0
greenlet>=3.0.0                  # required by SQLAlchemy async on some python versions

# ── Security ─────────────────────────────────────────────────────────────────
# passlib was removed: passlib 1.7.4 (2020, last release) cannot detect bcrypt
# >= 4.1 and its internal self-test raises ValueError on any hash() call, so
# password hashing silently broke on a clean install. We call bcrypt directly
# (Backend/passwords.py). Do not re-add passlib.
bcrypt>=4.1,<6
cryptography>=42

# ── Data import / export ─────────────────────────────────────────────────────
openpyxl>=3.1                  # .xlsx parsing for the migration tool

# ── Tests (optional: pip install -r requirements.txt then run pytest) ────────
pytest>=8.0
pytest-asyncio>=0.23
