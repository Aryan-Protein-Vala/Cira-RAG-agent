# ── Web framework ────────────────────────────────────────────────────────────
fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.7
python-multipart>=0.0.9          # required by FastAPI for file uploads

# ── Persistence ──────────────────────────────────────────────────────────────
SQLAlchemy>=2.0                  # was missing: the app could not start without it
aiosqlite>=0.20                  # async SQLite driver used by SQLAlchemy

# ── Agent / LLM ──────────────────────────────────────────────────────────────
langchain>=0.3
langchain-core>=0.3
langchain-openai>=0.2
langgraph>=0.2

# ── SAP connectivity ─────────────────────────────────────────────────────────
hdbcli>=2.19                     # SAP HANA client (direct SQL, port 3<inst>15/30013)
httpx>=0.27                      # SAP B1 Service Layer (OData, port 50000)

# ── Misc ─────────────────────────────────────────────────────────────────────
python-dotenv>=1.0

# ── Tests (optional: pip install -r requirements.txt then run pytest) ────────
pytest>=8.0
pytest-asyncio>=0.23
